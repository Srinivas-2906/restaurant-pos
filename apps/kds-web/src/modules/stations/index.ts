/**
 * Multi-station KDS routing — deferred.
 * Today the kitchen runs as a single queue per outlet (GET /kds/outlets/:id/queue).
 * Re-enable station picker / per-station boards here when expo routing is needed.
 */
export const STATIONS_MODULE_ENABLED = false;
